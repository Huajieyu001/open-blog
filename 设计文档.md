# 博客系统设计文档

## 一、技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端 | Nuxt 3 (Vue 3 + Vite) | 混合渲染：博客页 SSR，后台管理 SPA |
| UI | Tailwind CSS + Element Plus | Tailwind 用于博客前台，Element Plus 用于管理后台 |
| 编辑器 | Markdown-it + Tiptap | Markdown + 富文本双模式切换 |
| 后端 | Go + Gin | 轻量级，内存占用小（~20-50MB） |
| 数据库 | SQLite | 零配置，适合低流量，备份简单 |
| 图片存储 | 阿里云 OSS | 后端统一上传接口 |
| 鉴权 | JWT (golang-jwt) | 无状态 token 认证 |
| 构建 | Go Build (后端) + npm (前端) | |

## 二、项目结构

```
open-blog/
├── 设计文档.md                     # 设计文档（本文件）
├── blog-frontend/                  # Nuxt 3 前端
│   ├── nuxt.config.ts              # 路由规则（SSR/SPA 分拆）
│   ├── pages/
│   │   ├── index.vue               # 博客首页（SSR）
│   │   ├── article/[id].vue        # 文章详情（SSR）
│   │   ├── category/[slug].vue     # 分类列表（SSR）
│   │   ├── admin.vue               # 后台布局
│   │   └── admin/                  # 后台页面（全部 SPA）
│   │       ├── login.vue
│   │       ├── dashboard.vue
│   │       ├── article/create.vue  # 双模式编辑器
│   │       ├── article/edit/[id].vue
│   │       ├── article/list.vue
│   │       ├── category/index.vue
│   │       └── tag/index.vue
│   ├── components/
│   │   ├── blog/ (Header, Footer, ArticleCard, Pagination)
│   │   ├── admin/ (Sidebar)
│   │   └── common/ (ImageUpload, MarkdownEditor)
│   ├── composables/ (useApi, useAuth, useArticle)
│   └── middleware/auth.ts
│
├── blog-server/                    # Go 后端
│   ├── go.mod
│   ├── main.go                     # 入口
│   ├── config/                     # 配置
│   ├── controller/                 # 控制器
│   │   ├── admin/                  # 后台 API（需 JWT 鉴权）
│   │   └── pub/                    # 前台 API（公开）
│   ├── service/                    # 业务逻辑
│   ├── repository/                 # 数据访问
│   ├── model/                      # 数据模型
│   ├── middleware/                  # 中间件（JWT、CORS 等）
│   ├── utils/                      # 工具类
│   └── sql/                        # SQL 脚本
│       ├── schema.sql              # 建表语句
│       └── init-data.sql           # 初始数据
│
└── deploy/                         # 部署配置
    ├── nginx.conf                  # Nginx 配置
    ├── blog-server.service         # systemd 服务
    └── backup.sh                   # 备份脚本
```

## 三、数据库设计

### users — 管理员用户
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| username | TEXT UNIQUE | 用户名 |
| password | TEXT | BCrypt 加密密码 |
| nickname | TEXT | 昵称 |
| avatar | TEXT | 头像 URL |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### categories — 分类
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| name | TEXT | 分类名称 |
| slug | TEXT UNIQUE | URL 友好标识 |
| description | TEXT | 描述 |
| sort_order | INTEGER DEFAULT 0 | 排序 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### tags — 标签
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| name | TEXT | 标签名 |
| slug | TEXT UNIQUE | URL 友好标识 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### articles — 文章
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| title | TEXT | 标题 |
| slug | TEXT UNIQUE | URL 友好标识（可选） |
| summary | TEXT | 摘要 |
| content_md | TEXT | Markdown 原文 |
| content_html | TEXT | 渲染后的 HTML |
| cover_image | TEXT | 封面图 URL |
| status | INTEGER DEFAULT 0 | 0=草稿, 1=已发布 |
| is_top | INTEGER DEFAULT 0 | 0=否, 1=置顶 |
| view_count | INTEGER DEFAULT 0 | 阅读量 |
| category_id | INTEGER | 分类 ID |
| author_id | INTEGER | 作者 ID |
| publish_time | DATETIME | 发布时间 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### article_tag — 文章标签关联
| 字段 | 类型 | 说明 |
|------|------|------|
| article_id | INTEGER PK | 文章 ID |
| tag_id | INTEGER PK | 标签 ID |
| created_at | DATETIME | 创建时间 |

## 四、API 设计

### 公开接口（无需鉴权）
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/pub/articles | 文章列表（分页、可按分类/标签/关键词筛选） |
| GET | /api/pub/articles/{id} | 文章详情（含完整 HTML） |
| GET | /api/pub/articles/slug/{slug} | 按 slug 获取文章 |
| GET | /api/pub/categories | 分类列表 |
| GET | /api/pub/tags | 标签列表 |

### 管理接口（需 JWT 鉴权）
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/admin/login | 管理员登录 |
| GET | /api/admin/articles | 文章列表（含草稿） |
| POST | /api/admin/articles | 创建文章 |
| PUT | /api/admin/articles/{id} | 更新文章 |
| DELETE | /api/admin/articles/{id} | 删除文章 |
| GET | /api/admin/categories | 分类列表 |
| POST | /api/admin/categories | 创建分类 |
| PUT | /api/admin/categories/{id} | 更新分类 |
| DELETE | /api/admin/categories/{id} | 删除分类 |
| GET | /api/admin/tags | 标签列表 |
| POST | /api/admin/tags | 创建标签 |
| PUT | /api/admin/tags/{id} | 更新标签 |
| DELETE | /api/admin/tags/{id} | 删除标签 |
| POST | /api/admin/upload | 上传图片到 OSS |

## 五、SSR/SPA 路由分割

```ts
// nuxt.config.ts
routeRules: {
  '/admin/**': { ssr: false },   // 管理后台纯 SPA，无需 SEO
  '/**': { ssr: true },          // 博客前台 SSR
}
```

## 六、鉴权流程

1. 前端 POST `/api/admin/login` 提交用户名密码
2. 后端验证后返回 JWT token（24h 有效）
3. 前端存 token 到 localStorage，后续请求带 `Authorization: Bearer <token>`
4. JWT 中间件拦截请求，解析 token 并注入上下文
5. `/api/admin/**` 路径默认要求认证，`/api/pub/**` 全部公开

## 七、部署架构

### 服务器 1（主服务器 - 2C2G）
```
├── Nginx (反向代理，静态文件服务)
├── 前端 Nuxt3 构建产物（静态部署）
├── 后端 Go 服务
└── SQLite 数据库
```

### 服务器 2（备份/监控 - 2C2G）
```
├── 备份脚本（定时备份数据库和文件）
├── 监控服务（可选）
└── 日志收集（可选）
```

### 资源分配估算
- Nginx: ~50MB 内存
- Go 后端: ~20-50MB 内存
- SQLite: ~10MB 内存
- 系统: ~200MB 内存
- **总计: ~300-400MB 内存**，剩余 1.6GB 用于缓存和突发流量

## 八、初始管理员

- 用户名: `admin`
- 密码: `admin123`（BCrypt 加密存储，首次登录后请修改）

## 九、成本估算

**阿里云 OSS 费用：**
- 存储：0.12 元/GB/月
- 流量：0.50 元/GB
- 请求：0.01 元/万次

**服务器费用：**
- 2C2G 云服务器：约 50-100 元/月/台
- 2 台服务器：约 100-200 元/月

**总成本：** 约 100-200 元/月（不含域名和 SSL 证书）
